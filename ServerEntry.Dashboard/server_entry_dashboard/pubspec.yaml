name: server_entry_dashboard
description: "Web page dashboard for your servers"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.2+2-fix

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  get: ^4.6.6
  cherrilog: ^0.2.0
  community_material_icon: ^5.9.55
  http: ^1.2.1
  flutter_staggered_grid_view: ^0.7.0
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
